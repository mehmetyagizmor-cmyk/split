# Vite ortam değişkenleri BUILD ANINDA koda gömülür (runtime'da okunmaz) —
# Vercel'de deploy etmeden ÖNCE bu değeri gerçek backend URL'ine ayarlamayı
# unutma, yoksa production build'i localhost'a istek atmaya devam eder.
VITE_API_URL=http://localhost:4000/api
